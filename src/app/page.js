export default function Page() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: 32, lineHeight: 1.5 }}>
      <h1>📦 Peleka Courier API (v2.0)</h1>
      <p>Backend API for the Customer app, Rider app, and Admin dashboard.</p>
      <p>Base path: <code>/api</code></p>
      <p>See <code>README.md</code> for setup, auth flow, and full route list.</p>
    </main>
  );
}
